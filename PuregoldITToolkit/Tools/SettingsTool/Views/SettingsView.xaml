<UserControl x:Class="PuregoldITToolkit.Tools.SettingsTool.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Global Settings &amp; Configurations" FontSize="24" FontWeight="Light" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,15"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Global Email Signature" FontWeight="Bold" Foreground="{StaticResource BrandPrimary}" Margin="0,0,0,5" FontSize="16"/>
                        <TextBlock Text="Paste your raw HTML signature below. This will be automatically injected at the bottom of all emails generated by the Service Desk and Forms tools." 
                                   Foreground="{StaticResource TextSecondary}" Margin="0,0,0,15" TextWrapping="Wrap"/>

                        <TextBox Text="{Binding SettingsData.SignatureHtml, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="250" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" 
                                 FontFamily="Consolas" FontSize="13" Background="{StaticResource BgApp}" Padding="12"/>

                        <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                            <Button Command="{Binding SaveSignatureCommand}" Content="SAVE GLOBAL SIGNATURE" Style="{StaticResource SuccessButton}" Padding="20,12" Margin="0,0,10,0"/>
                            <Button Command="{Binding ClearSignatureCommand}" Content="CLEAR" Background="{StaticResource TextSecondary}" Foreground="#FFFFFF" FontWeight="Bold" Padding="20,12" BorderThickness="0" Cursor="Hand"/>
                        </StackPanel>

                        <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource BrandPrimary}" FontWeight="Bold" Margin="0,15,0,0"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>