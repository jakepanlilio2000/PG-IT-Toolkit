<UserControl x:Class="PuregoldITToolkit.Tools.SodChecker.Views.SodCheckerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:PuregoldITToolkit.Tools.SodChecker.Models">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="SegmentedTab" TargetType="RadioButton">
            <Setter Property="Background" Value="#2F3640"/>
            <Setter Property="Foreground" Value="#7F8FA6"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="4" Margin="0,0,5,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#353B48"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#0097E6"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="StatusCellTemplate" DataType="{x:Type models:FileCheckStatus}">
            <Border x:Name="BgBorder" CornerRadius="4" Padding="5" Margin="2">
                <StackPanel>
                    <TextBlock x:Name="TxtStatus" Text="NO" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" FontSize="11"/>
                    <TextBlock x:Name="TxtMeta" Text="{Binding FormattedSize}" Foreground="#E0E0E0" HorizontalAlignment="Center" FontSize="9" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding State}" Value="Missing">
                    <Setter TargetName="BgBorder" Property="Background" Value="#E74C3C"/>
                    <Setter TargetName="TxtStatus" Property="Text" Value="NO"/>
                    <Setter TargetName="TxtMeta" Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Ok">
                    <Setter TargetName="BgBorder" Property="Background" Value="#2ECC71"/>
                    <Setter TargetName="TxtStatus" Property="Text" Value="YES ✓"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Rerun">
                    <Setter TargetName="BgBorder" Property="Background" Value="#F39C12"/>
                    <Setter TargetName="TxtStatus" Property="Text" Value="RERUN ↻"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Empty">
                    <Setter TargetName="BgBorder" Property="Background" Value="#F1C40F"/>
                    <Setter TargetName="TxtStatus" Property="Text" Value="EMPTY ⚠"/>
                    <Setter TargetName="TxtStatus" Property="Foreground" Value="Black"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Skipped">
                    <Setter TargetName="BgBorder" Property="Background" Value="#BDC3C7"/>
                    <Setter TargetName="TxtStatus" Property="Text" Value="SKIPPED"/>
                    <Setter TargetName="TxtMeta" Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal">
                    <RadioButton Content="Daily Overview" IsChecked="{Binding IsModeDaily}" Style="{StaticResource SegmentedTab}"/>
                    <RadioButton Content="Store History" IsChecked="{Binding IsModeHistory}" Style="{StaticResource SegmentedTab}"/>
                    <RadioButton Content="Targeted Column" IsChecked="{Binding IsModeTargeted}" Style="{StaticResource SegmentedTab}"/>
                </StackPanel>
            </StackPanel>

            <Border Grid.Row="1" Background="#F5F6FA" CornerRadius="6" Padding="15" Margin="0,0,0,15" BorderBrush="#DCDDE1" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Visibility="{Binding IsModeDaily, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="Target Date:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <DatePicker SelectedDate="{Binding TargetDate}" Width="150"/>
                        <TextBlock Text="* Scans all stores from your config for the selected date." Foreground="#7F8FA6" FontStyle="Italic" VerticalAlignment="Center" Margin="15,0,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Visibility="{Binding IsModeHistory, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="Select Store:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <ComboBox ItemsSource="{Binding AvailableStores}" SelectedItem="{Binding SelectedHistoryStore}" DisplayMemberPath="StoreName" Width="150" Margin="0,0,15,0"/>

                        <TextBlock Text="Date Range:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <DatePicker SelectedDate="{Binding StartDate}" Width="120" Margin="0,0,10,0"/>
                        <TextBlock Text="to" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <DatePicker SelectedDate="{Binding EndDate}" Width="120"/>
                    </StackPanel>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Visibility="{Binding IsModeTargeted, Converter={StaticResource BoolToVis}}">

                        <TextBlock Text="Specific Log:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <ComboBox ItemsSource="{Binding AvailableColumns}" SelectedItem="{Binding SelectedColumn}" Width="100" Margin="0,0,15,0" Padding="5,2"/>

                        <TextBlock Text="Date Range:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <DatePicker SelectedDate="{Binding StartDate}" Width="110" Margin="0,0,10,0"/>
                        <TextBlock Text="to" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <DatePicker SelectedDate="{Binding EndDate}" Width="110"/>

                        <TextBlock Text="* Scans a single column across all stores and dates." Foreground="#7F8FA6" FontStyle="Italic" VerticalAlignment="Center" Margin="15,0,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Command="{Binding ToggleStoreManagerCommand}" Content="⚙️ CONFIG" Background="#7F8FA6" Foreground="White" FontWeight="Bold" Padding="15,8" Margin="0,0,10,0" BorderThickness="0" Cursor="Hand"/>
                        <Button Command="{Binding ScanCommand}" Content="START SCAN" Background="#44BD32" Foreground="White" FontWeight="Bold" Padding="25,8" BorderThickness="0" Cursor="Hand"/>
                    </StackPanel>
                </Grid>
            </Border>

            <StackPanel Grid.Row="2" Margin="0,0,0,10">
                <TextBlock Text="{Binding StatusMessage}" Foreground="#0097E6" FontWeight="Bold" FontSize="11" Margin="0,0,0,5"/>
                <ProgressBar Value="{Binding ScanProgress}" Height="4" Background="#DCDDE1" Foreground="#44BD32" BorderThickness="0" Maximum="100"/>
            </StackPanel>

            <DataGrid Grid.Row="3" ItemsSource="{Binding StoreResults}" AutoGenerateColumns="False" CanUserAddRows="False" 
                      HeadersVisibility="Column" Background="White" RowBackground="White" AlternatingRowBackground="#F5F6FA"
                      BorderBrush="#DCDDE1" BorderThickness="1" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#ECF0F1">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID / Date" Binding="{Binding DisplayId}" FontWeight="Bold" Width="90" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Name / Day" Binding="{Binding DisplayName}" Width="160" IsReadOnly="True"/>

                    <DataGridTemplateColumn Header="EJ" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding EjStatus}" ContentTemplate="{StaticResource StatusCellTemplate}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Pollog" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding PollogStatus}" ContentTemplate="{StaticResource StatusCellTemplate}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="CRM" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding CrmStatus}" ContentTemplate="{StaticResource StatusCellTemplate}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Promo" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding PromoStatus}" ContentTemplate="{StaticResource StatusCellTemplate}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="BIR" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding BirStatus}" ContentTemplate="{StaticResource StatusCellTemplate}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="NonTrade" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding NonTradeStatus}" ContentTemplate="{StaticResource StatusCellTemplate}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="DOS Log" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding DosStatus}" ContentTemplate="{StaticResource StatusCellTemplate}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Mob Prc" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding MobilePriceStatus}" ContentTemplate="{StaticResource StatusCellTemplate}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="PlusKu" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding PlusKuPriceStatus}" ContentTemplate="{StaticResource StatusCellTemplate}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Reg Prc" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding RegPriceStatus}" ContentTemplate="{StaticResource StatusCellTemplate}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="SIS98" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding Sis98Status}" ContentTemplate="{StaticResource StatusCellTemplate}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="ShelfT" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding ShelftagStatus}" ContentTemplate="{StaticResource StatusCellTemplate}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Kiosk" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding KioskStatus}" ContentTemplate="{StaticResource StatusCellTemplate}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Grid Background="#99000000">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsStoreManagerOpen}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <Border Background="White" Width="550" Height="450" CornerRadius="8" Padding="20" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="15"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="Store Configuration Manager" FontSize="18" FontWeight="Bold" Foreground="#2F3640"/>
                        <Button Command="{Binding ToggleStoreManagerCommand}" Content="✕" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" FontSize="16" Cursor="Hand" Foreground="#7F8FA6"/>
                    </Grid>

                    <Border Grid.Row="1" Background="#F5F6FA" Padding="10" CornerRadius="6" Margin="0,0,0,15">
                        <WrapPanel VerticalAlignment="Center">
                            <TextBlock Text="Code:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="SemiBold"/>
                            <TextBox Text="{Binding NewStore.StoreCode, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="0,0,10,0"/>

                            <TextBlock Text="Name:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="SemiBold"/>
                            <TextBox Text="{Binding NewStore.StoreName, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="0,0,10,0"/>

                            <TextBlock Text="Type:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="SemiBold"/>
                            <TextBox Text="{Binding NewStore.StoreType}" Width="70" Margin="0,0,10,0"/>

                            <Button Command="{Binding AddStoreCommand}" Content="+ ADD STORE" Background="#44BD32" Foreground="White" FontWeight="Bold" Padding="10,5" BorderThickness="0" Cursor="Hand"/>
                        </WrapPanel>
                    </Border>

                    <DataGrid Grid.Row="2" ItemsSource="{Binding AvailableStores}" AutoGenerateColumns="False" CanUserAddRows="False" 
                              HeadersVisibility="Column" Background="White" RowBackground="White" AlternatingRowBackground="#F5F6FA"
                              BorderBrush="#DCDDE1" BorderThickness="1" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#ECF0F1">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Code" Binding="{Binding StoreCode}" FontWeight="Bold" Width="60" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Store Name" Binding="{Binding StoreName}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding StoreType}" Width="80" IsReadOnly="True"/>
                            <DataGridTemplateColumn Header="Action" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Delete" Command="{Binding DataContext.DeleteStoreCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Background="#E74C3C" Foreground="White" BorderThickness="0" Padding="5,2" Cursor="Hand"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>