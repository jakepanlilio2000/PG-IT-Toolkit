<UserControl x:Class="PuregoldITToolkit.Tools.FormsTool.Views.FormsMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModels="clr-namespace:PuregoldITToolkit.Tools.FormsTool.ViewModels"
             xmlns:views="clr-namespace:PuregoldITToolkit.Tools.FormsTool.Views">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewModels:InfViewModel}">
            <views:InfView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:ObViewModel}">
            <views:ObView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:SsrfViewModel}">
            <views:SsrfView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModels:TsrfViewModel}">
            <views:TsrfView/>
        </DataTemplate>

        <Style x:Key="SegmentedTab" TargetType="RadioButton">
            <Setter Property="Background" Value="{StaticResource BgApp}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}" 
                                CornerRadius="4" 
                                Margin="0,0,8,0"
                                BorderBrush="{StaticResource BorderLight}"
                                BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BgHighlight}"/>
                                <Setter Property="Foreground" Value="{StaticResource BrandPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="{StaticResource BrandPrimary}"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                <Setter Property="BorderBrush" Value="{StaticResource BrandPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="1" Background="{StaticResource BgCard}" Padding="20,15,20,10" BorderBrush="{StaticResource BorderLight}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <RadioButton Content="🛒 INF (Item Not Found)" IsChecked="{Binding IsInfSelected}" Style="{StaticResource SegmentedTab}"/>
                <RadioButton Content="💼 OB (Official Business)" IsChecked="{Binding IsObSelected}" Style="{StaticResource SegmentedTab}"/>
                <RadioButton Content="⚙️ SSRF (System Service)" IsChecked="{Binding IsSsrfSelected}" Style="{StaticResource SegmentedTab}"/>
                <RadioButton Content="🔧 TSRF (Tech Service)" IsChecked="{Binding IsTsrfSelected}" Style="{StaticResource SegmentedTab}"/>
            </StackPanel>
        </Border>

        <ContentControl Grid.Row="2" Content="{Binding CurrentViewModel}" Padding="15" />
    </Grid>
</UserControl>