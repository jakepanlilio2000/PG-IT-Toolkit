<UserControl x:Class="PuregoldITToolkit.Tools.FormsTool.Views.InfView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Item Not Found (INF) Generator" FontSize="24" FontWeight="Light" Foreground="#2F3640" Margin="0,0,0,15"/>

        <!-- DATA ENTRY FORM -->
        <Border Grid.Row="1" Background="#F5F6FA" CornerRadius="6" Padding="15" Margin="0,0,0,15" BorderBrush="#DCDDE1" BorderThickness="1">
            <StackPanel>
                <WrapPanel Margin="0,0,0,15">
                    <TextBlock Text="INF Form Type:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox ItemsSource="{Binding InfTypes}" SelectedItem="{Binding SelectedInfType}" Width="320" Padding="5,2"/>
                </WrapPanel>

                <WrapPanel>
                    <StackPanel Margin="0,0,15,10">
                        <TextBlock Text="Store Code *" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding CurrentEntry.StoreCode, UpdateSourceTrigger=PropertyChanged}" Width="70" Padding="5"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,10">
                        <TextBlock Text="SKU *" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding CurrentEntry.Sku, UpdateSourceTrigger=PropertyChanged}" Width="100" Padding="5"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,10" Visibility="{Binding IsType4Apar, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="Gen. SKU (APAR) *" FontWeight="SemiBold" Foreground="#E67E22"/>
                        <TextBox Text="{Binding CurrentEntry.GeneratedSku, UpdateSourceTrigger=PropertyChanged}" Width="120" Padding="5" BorderBrush="#E67E22"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,10">
                        <TextBlock Text="UPC" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding CurrentEntry.Upc, UpdateSourceTrigger=PropertyChanged}" Width="130" Padding="5"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,10">
                        <TextBlock Text="PurePOS Price" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding CurrentEntry.PurePosPrice, UpdateSourceTrigger=PropertyChanged}" Width="100" Padding="5"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,10">
                        <TextBlock Text="MMS Price" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding CurrentEntry.MmsPrice, UpdateSourceTrigger=PropertyChanged}" Width="100" Padding="5"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,10">
                        <TextBlock Text="Description" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding CurrentEntry.Description, UpdateSourceTrigger=PropertyChanged}" Width="200" Padding="5"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,15,10">
                        <TextBlock Text="Is Promo?" FontWeight="SemiBold"/>
                        <TextBox Text="{Binding CurrentEntry.IsPromo, UpdateSourceTrigger=PropertyChanged}" Width="70" Padding="5" IsReadOnly="{Binding IsPromoLocked}"/>
                    </StackPanel>

                    <Button Command="{Binding AddToTableCommand}" Content="+ ADD TO LIST" Background="#44BD32" Foreground="White" FontWeight="Bold" Padding="15,5" Margin="0,15,0,0" BorderThickness="0" Cursor="Hand" Height="30"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- DATA GRID PREVIEW -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding InfTable}" AutoGenerateColumns="False" CanUserAddRows="False" 
                  HeadersVisibility="Column" Background="White" RowBackground="White" AlternatingRowBackground="#F5F6FA"
                  BorderBrush="#DCDDE1" BorderThickness="1" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#ECF0F1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Store" Binding="{Binding StoreCode}" FontWeight="Bold" Width="60"/>
                <DataGridTextColumn Header="Reg. SKU" Binding="{Binding Sku}" Width="100"/>

                <!-- Visibility controlled by ElementStyle -->
                <DataGridTextColumn Header="Generated SKU (APAR)" Binding="{Binding GeneratedSku}" Width="140">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DataContext.IsType4Apar, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="UPC" Binding="{Binding Upc}" Width="130"/>
                <DataGridTextColumn Header="PurePos Price" Binding="{Binding PurePosPrice}" Width="100"/>
                <DataGridTextColumn Header="MMS Price" Binding="{Binding MmsPrice}" Width="100"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Is Promo?" Binding="{Binding IsPromo}" Width="80"/>

                <DataGridTemplateColumn Header="Action" Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="X" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Background="#E74C3C" Foreground="White" BorderThickness="0" Padding="5,2" Cursor="Hand"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- FOOTER / EXPORT -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#2980B9" FontWeight="Bold" VerticalAlignment="Center"/>

            <Button Command="{Binding ExportCommand}" Content="EXPORT TO WORD" HorizontalAlignment="Right" Background="#273C75" Foreground="White" FontWeight="Bold" Padding="25,10" BorderThickness="0" Cursor="Hand"/>
        </Grid>
    </Grid>
</UserControl>